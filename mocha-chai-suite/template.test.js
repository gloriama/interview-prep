var chai = require('chai');
var assert = chai.assert;

describe('chai', function() {
  describe('warmup', function() {
    it('should be able to do a simple assert', function() {
      assert(1 === 1);
    });
  });
});